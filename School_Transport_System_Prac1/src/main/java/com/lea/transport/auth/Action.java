package com.lea.transport.auth;

/** Every distinct, permissioned action exposed by the system. */
public enum Action {
    IMPORT_PUPIL_DATA,
    EDIT_PUPIL_DETAILS,
    CORRECT_ANY_DATA,
    MANAGE_USER_ACCOUNTS,
    MANAGE_BUS_CONTRACT,
    REASSIGN_PUPIL_ROUTE,
    VIEW_BUS_ROUTE,
    GENERATE_REPORT
}
